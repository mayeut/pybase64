name: Valgrind

on:
  push:
    branches-ignore:
      - "dependabot/**"
  pull_request:

jobs:
  build_sdist:
    name: Valgrind
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive

      - uses: deadsnakes/action@v3.0.0
        name: Install Python
        with:
          python-version: "3.10"
          debug: true

      - name: Install dependencies
        run: |
          set -exuo pipefail
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends valgrind
          python -m pip install -r requirements-test.txt

      - name: Run valgrind tests
        run: |
          set -exuo pipefail
          CFLAGS="-O0" CIBUILDWHEEL=1 python -m pip install -e .
          PYTHONMALLOC=malloc valgrind --leak-check=full --show-leak-kinds=definite --errors-for-leak-kinds=definite --error-exitcode=2 $(which python) -m pytest
